apiVersion: v1
kind: Service
metadata:
  name: {{ include "feature-flag-manager.fullname" . }}
  labels:
    {{- include "feature-flag-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "feature-flag-manager.selectorLabels" . | nindent 4 }}
